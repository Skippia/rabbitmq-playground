USERNAME="rmuser"
PASSWORD="rmpassword"
HOSTNAME="rabbitmq"
# HOSTNAME="localhost"
PORT=5672
QUEUE="q.external"
QUEUE_NORMAL="q.external-normal"
COUNT=10000000
# COUNT=100
MAX_RETRIES=2
MAX_CONCURRENT_HANDLERS=300
RETRIES_PERCENTAGE=10

SRC_URI=amqp://${USERNAME}:${PASSWORD}@${HOSTNAME}
